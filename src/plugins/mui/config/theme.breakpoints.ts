import { Breakpoints } from '@mui/material/styles'

const breakpoints: Partial<Breakpoints> = {
  values: {
    xs: 0,
    sm: 560,
    md: 800,
    lg: 1140,
    xl: 1300,
  },
}

export { breakpoints }
